apiVersion: v1
kind: Service
metadata:
  name: {{ include "j17-mvn-rel-import-hub-bld.fullname" . }}
  labels:
    {{- include "j17-mvn-rel-import-hub-bld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "j17-mvn-rel-import-hub-bld.selectorLabels" . | nindent 4 }}
